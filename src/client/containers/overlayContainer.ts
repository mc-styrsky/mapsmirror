import { createHTMLElement } from '../createHTMLElement';

export const overlayContainer = createHTMLElement({
  style: {
    left: '0px',
    position: 'absolute',
    top: '0px',
  },
  tag: 'div',
  zhilds: [],
});
