import { createHTMLElement } from '../../../utils/createHTMLElement';

export const submit = createHTMLElement({
  classes: ['btn', 'btn-primary'],
  tag: 'button',
  type: 'submit',
  zhilds: ['Goto'],
});
