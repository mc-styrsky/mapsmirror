import { createHTMLElement } from '../createHTMLElement';

export const mapContainer = createHTMLElement({
  style: {
    left: '0px',
    position: 'absolute',
    top: '0px',
  },
  tag: 'div',
  zhilds: [],
});
